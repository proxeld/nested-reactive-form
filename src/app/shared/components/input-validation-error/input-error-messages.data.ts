
export const ERROR_MESSAGES = {
  required: 'This field is required.',
  minlength: 'Min length is at list { requiredLength } characters',
  maxlength: 'Max length is at most { requiredLength } characters',
  email: 'Must comply to email format'
};
